let contadora = 1

while (contadora<=200) {
    if (contadora %2 == 0) {
        console.log ("O numero mostrado é: " + contadora + " e ele é par")    
    } else {
        console.log ("O numero mostrado é: " + contadora + " e ele é impar")
    }
    
    contadora++
}